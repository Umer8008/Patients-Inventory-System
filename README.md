# Patients-Inventory-System
FastAPI-based Patient Management System providing RESTful CRUD operations with persistent JSON storage. Uses Pydantic for data validation and automatically calculates patient BMI and weight verdict based on height and weight.
